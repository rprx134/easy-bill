import React from 'react';
import SiteHeader from '../SiteHeader/SiteHeader';
import QuickOperation from '../QuickOperation/QuickOperation';
const dashboard = () => {
    return (
        <div>
            <SiteHeader />
            <QuickOperation/>
        </div>
    );
}

export default dashboard;